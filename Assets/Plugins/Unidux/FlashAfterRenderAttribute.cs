namespace Unidux
{
    // Attribute for identify one-time state property
    // FlashAfterRenderAttribute only supports nullable type.
    public class FlashAfterRenderAttribute : System.Attribute
    {
        public FlashAfterRenderAttribute()
        {
        }
    }
}
